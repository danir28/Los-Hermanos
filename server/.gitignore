node_modules
# Keep environment variables out of version control
.env
.env.test

/src/generated/prisma

# Fotos de producto subidas desde el admin (ver src/products/uploads.ts) — viven en disco del
# droplet de producción, no en git.
/uploads
